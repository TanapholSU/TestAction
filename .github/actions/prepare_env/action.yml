name: Greeter
description: Composite Action to greet someone

on:
  workflow_dispatch:

# inputs:
#   who-to-greet:
#     required: false
#     default: World
#     description: who to greet

runs:
  using: composite
  steps:
    - name: greet
      run: echo "Hello world"
      shell: bash
